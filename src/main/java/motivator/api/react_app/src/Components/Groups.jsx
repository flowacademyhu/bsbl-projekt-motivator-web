/*
import React, { Component } from 'react';
import axios from 'axios';

class Groups extends Component {
  state = {
      groupName: '',
      groupAdmin: ''
    };
  
    getGroups = () => {

    }
  
  }



const Groups = (props) => {
  const items = props.groups.map((item) => {
    return (
      <Groups key={item.id} item={item} />
    );
  });

  return (
    <div>
      <h3>{item.name}</h3>
      <div>{item.admin}</div>
    </div>
  );
};

export default Groups;
*/
