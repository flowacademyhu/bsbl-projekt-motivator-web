spring:
  datasource:
    url: jdbc:mysql://localhost:3306/motivator?useSSL=false
    username: root
    password: toor
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect